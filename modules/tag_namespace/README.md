# OCI Tag Namespace Module

Creates an OCI defined-tag namespace and optional tag definitions. Tags support
cost tracking, retirement, and OCI validators such as an `ENUM` list.

Outputs are `id`, `tag_ids`, and `tag_names`. See [EXAMPLES.md](./EXAMPLES.md).
