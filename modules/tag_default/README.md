# OCI Tag Default Module

Creates one OCI tag default for a compartment and tag definition. When
`is_required` is true, OCI rejects creation of resources that cannot accept the
tag default.

Outputs are `id` and `tag_definition_name`. See [EXAMPLES.md](./EXAMPLES.md).
