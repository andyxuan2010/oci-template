# OCI Compartment Quota Module

Creates an OCI quota policy from explicit quota statements. Quota syntax and
service-family names are controlled by OCI and should be verified against the
target tenancy before apply.

Outputs are `id` and `name`. See [EXAMPLES.md](./EXAMPLES.md).
